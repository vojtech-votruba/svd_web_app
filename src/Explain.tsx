function Explain () {
    return (
        <>
            <hr></hr>
            <p>1: Every image can be represented as a matrix. A rank of a matrix is the number of its linearly independent columns. In layman's terms, the lesser the rank of the matrix is, the simpler the matrix is. In our specific example: A larger rank implies less compression.</p>
        </>
    );
}

export default Explain;